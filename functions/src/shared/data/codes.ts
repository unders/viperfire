class Auth {
    static readonly NetworkRequestFailed = "auth/network-request-failed";
}

export class codes {
    // error
    static readonly FatalError = "FATAL-ERROR";
    static readonly TemporaryError = "TEMPORARY-ERROR";

    // auth
    static readonly auth = Auth;
}
